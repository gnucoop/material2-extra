import {BuildPackage, buildConfig} from 'm2e-build-tools';
import {join} from 'path';

export const calendarPackage = new BuildPackage('calendar');
export const masonryPackage = new BuildPackage('masonry');
